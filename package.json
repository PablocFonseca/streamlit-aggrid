{
  "name": "streamlit-aggrid",
  "packageManager": "yarn@4.1.0",
  "version": "0.3.3",
  "repository": "https://github.com/PablocFonseca/streamlit-aggrid.git",
  "license": "MIT",
  "author": "Pablo Fonseca",
  "private": true,
  "workspaces": [
    "streamlit_aggrid/frontend"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -Avi run build",
    "build_all": "yarn workspaces foreach -Avi run build && poetry build",
    "develop": "yarn workspaces foreach -R -v -j 99 -i -p run dev:watch"
  },
  "resolutions": {
    "streamlit-component-lib/apache-arrow": "9.0.0"
  },
  "devDependencies": {
    "webpack-bundle-analyzer": "^4.10.2",
    "webpack-cli": "^6.0.1"
  }
}
